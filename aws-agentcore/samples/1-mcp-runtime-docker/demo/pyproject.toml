[project]
name = "neo4j-mcp-runtime-demo"
version = "0.1.0"
description = "Demo client for testing deployed AWS AgentCore Neo4j MCP Runtime"
requires-python = ">=3.10"
dependencies = [
    "strands-agents",
    "strands-agents-tools",
    "mcp-proxy-for-aws",
    "boto3",
    "python-dotenv",
]
