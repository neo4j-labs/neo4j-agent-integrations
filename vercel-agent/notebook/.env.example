# ──────────────────────────────────────────────────────────────────────────────
# LLM Provider  — set AI_PROVIDER + the matching API key
# ──────────────────────────────────────────────────────────────────────────────

# OpenAI (default)
AI_PROVIDER=openai
AI_MODEL=gpt-5.4
OPENAI_API_KEY=your-openai-api-key

# Google Gemini  →  npm install @ai-sdk/google
# AI_PROVIDER=google
# AI_MODEL=gemini-2.0-flash
# GOOGLE_GENERATIVE_AI_API_KEY=your-google-api-key

# Anthropic Claude  →  npm install @ai-sdk/anthropic
# AI_PROVIDER=anthropic
# AI_MODEL=claude-3-5-sonnet-20241022
# ANTHROPIC_API_KEY=your-anthropic-api-key

# Mistral  →  npm install @ai-sdk/mistral
# AI_PROVIDER=mistral
# AI_MODEL=mistral-large-latest
# MISTRAL_API_KEY=your-mistral-api-key

# ──────────────────────────────────────────────────────────────────────────────
# Neo4j — Graph Data  (demo read-only database)
# ──────────────────────────────────────────────────────────────────────────────
NEO4J_URI=neo4j+s://demo.neo4jlabs.com:7687
NEO4J_USERNAME=companies
NEO4J_PASSWORD=companies
NEO4J_DATABASE=companies

# ──────────────────────────────────────────────────────────────────────────────
# Neo4j MCP Server
# MCP_URL: set to a hosted remote URL (e.g. https://mcp.example.com/mcp) or leave
#          blank to use the local MCP server started via the Jupyter notebook.
# MCP_PORT: local port when running neo4j-mcp-server (default: 8443)
# ──────────────────────────────────────────────────────────────────────────────
# MCP_URL=https://your-hosted-mcp-server.example.com/mcp
MCP_PORT=8443

# ──────────────────────────────────────────────────────────────────────────────
# Neo4j Agent Memory Service (NAMS)  — used by 3-memory-agent.mjs
# Get a free API key at https://memory.neo4jlabs.com
# ──────────────────────────────────────────────────────────────────────────────
MEMORY_API_KEY=nams_your-api-key-here
MEMORY_WORKSPACE_ID=ws_your-workspace-id-here
DEMO_AGENT_ID=vercel-neo4j-notebook-agent
# MEMORY_ENDPOINT=https://memory.neo4jlabs.com/v1  # optional: override default endpoint
